import React from 'react'
import { Text, View } from '../../components'
import styles from './styles'

/**
 *
 * Most likely will not be implemented for now
 * Too much unknowns on the api side
 *
 * the technology is set up on the api
 * It just requires how the email handling is done and from what email
 *
 * this page would run the forgot password mutation then unset cookie
 *
 */

const ForgotPasswordView = ({ navigation }) => {
  return <View></View>
}

export default ForgotPasswordView
